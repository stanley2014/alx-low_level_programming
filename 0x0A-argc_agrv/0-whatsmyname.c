#include <stdlib.h>
#include <stdio.h>

/*
 * main - wrute a program that prints its name.
 *
 * Return: 0
 */
int main(int argc, char *argv[])
{
	for(agrc >= 1)
	{
		printf("%s\n", argv[0]);
	}
	
	return 0;
}
